import React from 'react'; //next쓴다면 이 코드를 안써도 됨. 
import AppLayout from '../components/AppLayout';

const Home = () => {
    return(
        <AppLayout>
            <div>Hello, Next</div>
        </AppLayout>
    )
}

export default Home;